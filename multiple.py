def is_multiple(n,m):
    try:
        if n % m == 0:
            print " {0} is a multiple of {1}".format(n,m)
        else:
            print " {0} is Not a multiple of {1}".format(n,m)
            
